apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnet-resources-test
  labels:
    app: dotnet-resources-test
spec:
  selector:
    matchLabels:
      app: dotnet-resources-test
  template:
    metadata:
      labels:
        app: dotnet-resources-test
    spec:
      containers:
      - name: dotnet-resources-test
        image: sztestacr.azurecr.io/dotnet-resources-test:0.0.4
        resources:
          requests:
            memory: "560Mi"
            cpu: "100m"
          limits:
            memory: "1024Mi"
            cpu: "200m"
